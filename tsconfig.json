{
  "compilerOptions": {
    "target": "ESNext",
    "lib": [
      "ES2020",
      "DOM",
      "DOM.Iterable"
    ],
    "composite": true,
    "skipLibCheck": true, /* Skip type checking of declaration files. (.d.ts) */
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "allowSyntheticDefaultImports": true, /* Allow default imports */
    /* Bundler mode */
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx", /* Preferable way of JSX transformation to JS */
    "allowJs": false, /* Allow JavaScript files to be a part of your program. Use the 'checkJS' option to get errors from these files. */
    "types": [
      "vite/client"
    ],
    "baseUrl": ".", /* Let you set a base directory to resolve non-absolute module names - allow using aliases */
    "paths": {
      "@/*": [
        "src/*"
      ],
    },
    /* Linting */
    "strict": true, /* The strict flag enables a wide range of type checking behavior that results in stronger guarantees of program correctness. Turning this on is equivalent to enabling all of the strict mode family options */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true, /* Ensure case sensitivity rule so it won't come to conflict between case sensitive and case insensitive file systems */
  },
  "exclude": [
    "node_modules",
    "vite.config.js"
  ],
  "include": [
    ".eslintrc.cjs",
    "src/**/*",
  ],
}